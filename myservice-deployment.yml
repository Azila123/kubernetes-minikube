apiVersion: apps/v1
kind: Deployment
metadata:
  name: myservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myservice
  template:
    metadata:
      labels:
        app: myservice
    spec:
      containers:
        - image: clementt1/newimagename:latest
          imagePullPolicy: IfNotPresent
          name: myservice
      restartPolicy: Always
